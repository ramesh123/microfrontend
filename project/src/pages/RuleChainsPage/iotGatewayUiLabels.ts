/**
 * User-facing copy for IoT Gateway rule chains (`/iot-gateway/rulechains`; ThingsBoard APIs unchanged).
 * Aligns with menu `title: "IOT Gateway"` (`p_id` may still be `rulechain` from RBAC).
 */
export const IOT_GATEWAY_TITLE = "Rule Chains";

/** Lower / sentence-case helper for messages */
export const iotGatewayFlow = "Rule Chains";
export const iotGatewayFlows = "Rule Chains";

/** Fallback when a row has no name (dialogs / set-root). */
export const thisIotGatewayFlow = "Rule Chains";
