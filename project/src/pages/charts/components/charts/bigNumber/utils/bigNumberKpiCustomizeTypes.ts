/** KPI card glass / border options used by stream-style KPI surfaces. */
export type BigNumberKpiCustomizeOptions = {
  kpiGlassEffect?: boolean;
  kpiGlassOpacity?: number | string;
  kpiGlassBlur?: number | string;
  kpiBorderColor?: string;
  kpiGlassTintColor?: string;
  kpiCardBackgroundColor?: string;
  kpiCardColorScheme?: string;
  cardBackgroundColor?: string;
};
